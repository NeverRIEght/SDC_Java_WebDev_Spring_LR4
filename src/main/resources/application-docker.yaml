server:
  port: ${APP_INTERNAL_PORT}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOSTNAME}:${DB_INTERNAL_PORT}/${DB_NAME}
  rabbitmq:
    host: ${RABBITMQ_HOSTNAME}
    port: ${RABBITMQ_INTERNAL_PORT}
