spring:
  application:
    name: NotesApplication
  profiles:
    active: jdbc
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:notesdb;DB_CLOSE_DELAY=-1
    username: mkomarov
    password: